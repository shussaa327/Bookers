<h1>Books</h1>
<table>
	<tr>
		<th>Title</th>
		<th>Body</th>
		<th></th>
		<th></th>
		<th></th>
	</tr>
	<!-- 繰り返し処理 -->
	<% @books.each do |book| %>
	<tr>
		<td><%= book.title %></td>
		<td><%= book.body %></td>
		<td>show</td>
		<td>edit</td>
		<td>Destroy</td>
	</tr>
	<% end %>
	<!-- 繰り返し処理終了 -->
</table>
<!-- 新規投稿フォーム -->
<h1>New book</h1>

<%= form_for(@book) do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit "create Book" %>
<% end %>

